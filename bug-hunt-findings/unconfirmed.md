# Unconfirmed Candidates

Flagged in discovery but could NOT be reproduced or concretely demonstrated by the adversarial verifier (or were already fully fixed). Listed for transparency; none are in the active work queue.

| Location | Category | Why raised | Why verification failed |
|----------|----------|------------|--------------------------|
| `src/main/pty.ts:343 (sessions.set with no prior-occupancy check), 274-353 (spawn handler)` | resource-leak | pty:spawn overwrites an existing sessions entry without killing the prior proc, leaking th | The no-guard observation is real but produces no wrong behavior: there is no code path that issues pty:spawn for a still-occupied id. Every spawn is preceded by a kill/park whose cleanup synchronously does sessions.delete(id) before the spa |
| `src/main/preview.ts:315-323` | error-handling | preview:setBoundsBatch does not validate per-item bounds — one malformed item throws in ro | No reachable trigger in current code: every caller of preview:setBoundsBatch constructs items with fully-formed numeric bounds from boundsFor()/roundRect(), and there is no logic path that yields undefined/non-object bounds. The "crafted IP |
| `src/main/preview.ts:172-179` | error-handling | did-finish-load reads wc.getURL() outside the try/catch — throws if the webContents is bei | For getURL() to throw, the webContents must be in the isDestroyed()===true state when the synchronous did-finish-load JS handler body executes. The repro proves this state is never coincident with a did-finish-load dispatch: (1) webContents |
| `src/main/index.ts:210 (handler); root-cause interaction with src/main/preview.ts:299,376 and src/main/selfTest.ts:114` | error-handling | unhandledRejection handler hard-exits the whole app on routine preview loadURL rejections | Core premise is false: webContents.loadURL load-failure rejections do not propagate to process.on('unhandledRejection') in Electron 33 (the runtime consumes them and surfaces an 'electron: Failed to load URL' warning instead). The exact cit |
| `src/main/index.ts:15 (mainWindow let), 142-143 (getWin closures), 168-170 (activate)` | correctness | mainWindow is never reset to null on close and getWin() has no isDestroyed guard → handler | The cited grep facts are true but produce no reachable defect. getWin()-using handlers are renderer-only IPC, and a destroyed window has no renderer to call them; createWindow() reassigns mainWindow synchronously before any new renderer can |
| `src/main/index.ts:201-209 (crashing latch + crashShutdown + uncaughtException)` | error-handling | uncaughtException/unhandledRejection latch to app.exit(1) — any single benign uncaught err | The central premise is factually wrong for the project's runtime. Node does NOT "log-and-continue" on uncaughtException with no handler (it prints and exits 1), and unhandledRejection's default since Node 15 is 'throw' (also exits 1) — both |
| `src/main/index.ts:109-123` | correctness | CANVAS_SHOT did-finish-load handler always app.quit()s, hijacking a non-smoke run if CANVA | The capture-then-quit-without-CANVAS_SMOKE behavior is the documented contract of the CANVAS_SHOT dev tool (CLAUDE.md:115, code comment index.ts:106, stage-1 plan lines 731-732, features-and-testables.md:247). The candidate's proposed fix ( |
| `src/preload/index.ts:104-106` | error-handling | pty:port forwarder re-posts to '*' with no guard on empty/malformed port transfer | The defect is conditioned entirely on a hypothetical future change to MAIN. Against the current code, both (and only) senders always transfer exactly one valid MessagePortMain, so e.ports is never empty when pty:port is delivered. The crash |
| `src/preload/index.ts:46-47` | error-handling | spawnTerminal/openPreview typed to always resolve a result, but main throws (promise rejec | The claimed unhandled rejection cannot occur: src/main/index.ts assigns mainWindow exactly once (line 35) and never nulls it (no 'closed' handler; grep confirms a single assignment), so getWin() is non-null for the entire window lifetime. A |
| `src/preload/index.ts:105` | security | Privileged PTY MessagePort transferred with targetOrigin '*' and no consumer source/origin | The finding's security mechanism is incorrect. targetOrigin '*' does not control which in-page listeners receive a same-window self-post — all of them do, regardless — so the proposed scoped-origin and event.source hardening would not stop  |
| `src/preload/index.d.ts:1-9` | correctness | __ptyPort side-channel message contract is undeclared in the exposed type surface | No defect exists in the current code: producer and both consumers agree on { __ptyPort, id }, so ports bind correctly and terminals work. The candidate's trigger is an explicitly hypothetical future maintainer edit ("renames __ptyPort"), a  |
| `src/renderer/src/canvas/boards/BrowserPreviewLayer.tsx:302-313, 238-258` | concurrency | closeBoard leaves attachSeq unbumped and lastZoom stale, weakening the Bug #45 demote re-c | The fix's invariant ('every re-attachment bumps attachSeq') is maintained for the case that matters: the only path from a closed view (exists=false) back to attached is attachBoard's else-branch, which runs r.attachSeq++ (line 287) before r |
| `src/renderer/src/canvas/boards/BrowserPreviewLayer.tsx:618-624, 238-296, 346-372` | resource-leak | Unmount teardown closes all preview views but never cancels in-flight async attach/demote/ | The only non-benign sub-claim (openPreview re-creating an unclosable view post-teardown) requires preview:open to reach main AFTER preview:closeAll. That ordering inversion is impossible here: `await window.api.openPreview(...)` dispatches  |
| `src/renderer/src/canvas/boards/TerminalBoard.tsx:111-124 (attachWebgl), 126-133 (detachWebgl), 137-142 (lod effect)` | concurrency | Stale WebGL onContextLoss callback can null webglRef pointing at a freshly re-acquired (li | A disposed WebglAddon cannot fire its onContextLoss callback: xterm's Disposable.dispose() empties the EventEmitter's _listeners (clearListeners) and removes the underlying DOM webglcontextlost listener, so the captured closure registered i |
| `src/renderer/src/canvas/AppChrome.tsx:18 (OVERVIEW const) / 71 (overview button)` | correctness | "Overview" button omits maxZoom, so fitView zooms IN to 2.5x on small/single-board canvase | The candidate's confirming trigger and magnitude do not reproduce. It claims Overview zooms IN "up to 2.5x" "for the common single-board case," producing "inverted relative framing" vs Fit. The actual board dimensions (every default board i |
| `src/renderer/src/store/canvasStore.ts:129-141` | data-integrity | updateBoard sets changed=true and clears the redo branch for a value-identical patch (no r | The store mutators do clear future without a value diff, but that clear causes zero incremental data loss in any reachable path: the only production callers that could pass an identical value already call beginChange first (which clears fut |
| `src/renderer/src/store/history.ts:9-39` | data-integrity | applyUndo/applyRedo store the live boards array BY REFERENCE; selection nulling on undo ma | There is no current bug. Structural sharing of immutable snapshots is the standard, correct undo-stack pattern (same as Redux/Zustand history). All current mutators are immutable, verified by reading canvasStore.ts and elements.ts and by gr |
| `src/renderer/src/lib/boardSchema.ts:200-216, 336-342` | data-integrity | fromObject/migrate accept NaN (and other non-integer) schemaVersion as a valid current-ver | The logic gap is real but has no reachable trigger and the claimed harm cannot occur on current code. (1) The canvas.json.bak fallback the candidate says is bypassed does not exist — file I/O is Phase 3 / deferred, so there is nothing to by |
| `src/renderer/src/lib/boardSchema.ts:336-342` | data-integrity | Deep board validation runs BEFORE migration, which will reject legitimately-old docs once  | No current wrong behavior exists. With SCHEMA_VERSION=1 and an empty MIGRATIONS map, there is no older-version document whose boards could fail assertBoard before migration — every doc reaching fromObject is already at the current shape, wh |
| `src/renderer/src/lib/boardSchema.ts:305-307` | data-integrity | assertBoard accepts zero/negative board geometry (w/h) — no range check against MIN_BOARD_ | verifier-errored |
| `src/renderer/src/lib/previewPlan.ts:128-150` | correctness | chromeExclusionZones produces off-screen negative-coordinate zones when the pane is unmeas | The candidate's linchpin trigger - a Browser board at negative-coordinate world placement existing during the unmeasured-pane window - is not realizable in the current code. The store starts empty (no persistence/seed at construction; persi |
| `src/vendor/perfect-freehand/getStrokePoints.ts:104-149 (loop); getStrokeOutlinePoints.ts:247-400` | data-integrity | Non-finite (NaN/Infinity) input coordinates propagate unguarded into the outline, emitting | The defect is not reachable in current code. The candidate's own primary trigger (a persisted/migrated stroke with a NaN coordinate) is explicitly caught at the load boundary by fromObject/assertPlanningElement (boardSchema.ts:276) — which  |
| `src/vendor/perfect-freehand/getStrokeOutlinePoints.ts:278-280, 300-303, 424` | correctness | drawDot uses the pre-clamp firstRadius, bypassing the MIN_RADIUS floor the line path enfor | Not a real bug in this codebase: the faulty path is unreachable. The only caller fixes thinning:0 (radius=size/2=2, firstRadius=2, never sub-floor/negative) and never passes a custom easing; the single-point dot branch is gated off because  |
| `src/vendor/perfect-freehand/getStrokePoints.ts:65-71` | correctness | Two-point backfill drops the pressure channel, silently replacing input pressure with DEFA | Not a Canvas-ADE defect: the cited block is byte-for-byte identical to current upstream perfect-freehand (verified via WebFetch of the upstream main source), which this repo intentionally vendors verbatim per ADR 0001; the file was never mo |
| `src/main/preview.ts:327-332` | error-handling | preview:capture/capturePage rejection on close-during-await surfaces as an unhandled promi | The core premise — that webContents.close() during an in-flight capturePage() causes the promise to REJECT — is empirically false on the project's Electron 33.4.11. Direct experiment (capture-then-close at 0/1/3/10/50/100ms and same-tick) s |
| `src/main/pty.ts:133-138 (with onData at 303-315)` | data-integrity | adopt() can duplicate replayed scrollback: live onData deltas posted between sessions.set  | The finding rests on a misreading of JavaScript concurrency — it assumes an I/O (onData) callback can preempt a synchronous function mid-body between two adjacent statements. Under run-to-completion semantics that is impossible: adopt() run |
| `src/main/pty.ts:96-108` | resource-leak | park() is not idempotent: a second park for an id (or park after the proc already exited)  | The cited overwrite branch in park() is never executed: its guard sessions.get(id) (pty.ts:97-98) requires sessions[id], which by construction cannot be true while parked[id] is also populated (adopt consumes parked before populating sessio |
| `src/renderer/src/canvas/boards/TerminalBoard.tsx:202-218, 287-308` | concurrency | setState-after-unmount in TerminalBoard config/listShells effect is guarded, but the spawn | The defect requires a port.onmessage dispatch to execute term.write() AFTER term.dispose(), where both happen 'in the same teardown'. The teardown cleanup is a single synchronous block running as one event-loop task; Blink dispatches Messag |
| `src/renderer/src/canvas/boards/TerminalBoard.tsx:315-342` | resource-leak | Terminal Restart never re-fits the PTY and re-spawns past the deferred-fit/adopt machinery | The premise that term.cols/rows are stale at restart time is false: the persistent ResizeObserver keeps term.cols/rows in sync with the well layout via fit.fit()->term.resize() on every resize, and restart is a distinct later user gesture t |
| `src/main/index.ts:201-212` | resource-leak | Crash-path shutdown fires async PTY tree-kill then synchronously app.exit()s, so node-pty  | This is a re-report of already-fixed bug #47 as-is, which the task mandate forbids. #47's remediation deliberately and explicitly chose the synchronous, non-awaited crashShutdown shape (the code matches the prescribed fix verbatim, includin |
| `src/main/localServer.ts:63-72` | resource-leak | localServer.close() does not destroy in-flight keep-alive sockets, so server.close() can h | The factual core is wrong for the runtime: since Node 18.2.0 (and confirmed on Node 22.17 here via repro) server.close() immediately closes idle keep-alive connections, so the listener does not "linger until the socket idles out". It also c |
| `src/renderer/src/canvas/boards/BrowserPreviewLayer.tsx:238-258` | concurrency | demoteToSnapshot's existence re-check uses a stale rec reference captured before the captu | The candidate assumes the stale rec object R1 keeps attached=true after the delete, defeating the line-250 guard. In reality reconcile's removal branch calls closeBoard(id) (line 468) before recs.current.delete(id) (line 469), and closeBoar |
| `src/main/preview.ts:89-102` | security | External-open allowlist hands arbitrary external http(s) hosts from untrusted preview cont | The behavior is the intended, documented outcome of remediation #5/#23 (allowlist scoped to http/https/mailto), with the original file:/smb:/custom-scheme leak fully closed and the allow-http(s) contract locked by passing unit tests (previe |
| `src/renderer/src/store/canvasStore.ts:86-90, 120-142` | data-integrity | updateBoard('elements') path bypasses element validation, letting malformed planning eleme | No reachable code path produces a malformed planning element: all production and e2e callers use validated factories/typed immutable transforms, and screenToBoard guards against non-finite coordinates. The claimed consequence (save persists |
| `src/renderer/src/lib/boardSchema.ts:204-216` | correctness | Migration pipeline trusts each step to land exactly on the next version; an over-shooting  | Purely latent: with SCHEMA_VERSION=1 and an empty MIGRATIONS map the while-loop body is unreachable on every possible CanvasDoc, so no current input, code path, or caller produces an infinite loop, a wrong-version exit, a crash, or data los |
| `src/renderer/src/canvas/boards/TerminalBoard.tsx:265-273` | error-handling | Terminal spawn's adoptTerminal promise has no .catch(); a rejected pty:adopt leaves the te | In the current post-48c6500 code there is no reachable input/state/sequence that makes pty:adopt reject while a mounted, visible terminal board is present, so the claimed observable symptom (board silently stuck on 'spawning') cannot be dem |
