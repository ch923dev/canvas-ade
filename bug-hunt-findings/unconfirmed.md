# Unconfirmed Candidates

These were flagged in discovery but could NOT be confirmed under adversarial verification. None are in the active work queue.

| Location | Severity guess | Category | Why raised | Why refuted |
|----------|----------------|----------|------------|-------------|
| `src/main/selfTest.ts:114` | Medium | error-handling | void'd loadURL rejection in testPreview turns every preview-leg smoke failure into crashShutdown(1) — BUG-026's graceful degrade is dead code | REFUTED empirically on this repo's Electron 42: Electron internally attaches a no-op rejection handler to webContents.loadURL's promise, so a void'd rejection NEVER reaches process.on('unhandledRejection'). Proven by a temp-dir probe script run with the repo's electron.exe, unhandledRejection handler registered first. |
| `src/renderer/src/store/useMcpCommands.ts:53` | Medium | data-integrity | MCP background idle-reaper (and close_board) corrupt the user's undo/redo via tracked removeBoard | REFUTED as deliberate, test-enshrined design rather than a defect. The mechanics are real (applyMcpCommand 'removeBoard' → store.removeBoard at useMcpCommands.ts:53 → trackedChange pushes past + sets future:[] at canvasStore.ts:278-285; reaper path mcp.ts:55-57 → reapIdle), but tests enshrine this behavior. |
| `src/main/mcpOrchestrator.ts:343` | Medium | security | close_board can drain + tree-kill ANY board's PTY with no human confirm and no spawned-board restriction | REFUTED as design intent. The facts are accurate: closeBoard (mcpOrchestrator.ts:343-357) calls registry.drainPty then sendCommand removeBoard with no registry.confirm and no tracked.has(boardId) restriction — but this is the specified shape of the T3.2 lifecycle tool, not an inconsistency. |
| `src/main/mcpOrchestrator.ts:377` | Low | resource-leak | Idle-reaper never reaps boards stuck in failed/blocked/awaiting-review → permanent spawn-cap starvation | Refuted on the harm claim. The mechanics are accurately described (mcpOrchestrator.ts:377 `const idle = status === undefined || status === 'idle'`; a 'failed'/'blocked'/'awaiting-review' board resets idleSince at :379 and is never auto-reaped), but the claimed "permanent spawn-cap starvation" does not hold. |
| `src/renderer/src/canvas/RecapView.tsx:26` | Low | error-handling | RecapView refresh ignores { ok:false } and lets memory.refresh/readBoards rejections escape as unhandled promise rejections — silent refresh failure with no user feedback | Refuted on both legs. (1) The unhandled-rejection path does not exist: projectIpc.ts:360-368 awaits onRefresh, wired to summaryLoop.onIntent — and summaryLoop.ts wraps the ENTIRE doIntent body in try/catch (lines 469-563, catch → console.warn, non-fatal). |
| `src/renderer/src/canvas/AuditLogViewer.tsx:52` | Low | error-handling | AuditLogViewer.refresh has no .catch - mcp.readAudit rejection becomes an unhandled promise rejection with no user signal | REFUTED: the structural no-.catch is real (AuditLogViewer.tsx:49-53, `void refresh()` at 60 and 147), but every claimed rejection trigger is provably swallowed MAIN-side — the 'audit:read' handler (auditIpc.ts:30-36) returns [] on foreign sender — so the faulty path cannot be demonstrated. |
| `src/renderer/src/App.tsx:72` | Low | error-handling | Boot auto-reopen chain has no .catch despite its comment claiming the rejection is 'owned here' | REFUTED: the chain (App.tsx:72-79) indeed has no .catch and the inline comment overstates what returning applyOpenResult into the .then achieves — but every concrete rejection source the candidate names is provably non-rejecting, so the unhandled rejection cannot be demonstrated. |

## Dropped as prior-hunt re-reports (fix verified still present)

None.
