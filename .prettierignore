node_modules/
out/
dist/
release/
# Supabase Edge Functions (Deno) + SQL migrations — separate runtime, not app source.
supabase/
design-reference/
# Local Claude Code skill/plugin artifacts (mirrors the .claude/** eslint ignore).
.claude/
# Local Playwright MCP scratch (browser snapshots / console logs) — tool output, never reflow.
.playwright-mcp/
# Local impeccable design-hook cache (gitignored tool artifact) — never reflow.
.impeccable/
# Per-project runtime data dir (ADR 0009: canvas.json/assets/memory/tmp) — never source, never reflow.
.canvas/
# Vendored third-party source kept verbatim (ADR 0001 — perfect-freehand). Do not reflow.
src/vendor/
# Vendored Mermaid bundle for the diagram render worker (S4) — minified, never reflow.
resources/diagram-worker/mermaid.min.js
pnpm-lock.yaml
# Hand-authored docs (tables/prose) — keep Prettier's markdown reflow off them.
*.md
# Hand-tuned, token-accurate design mocks (throwaway HTML built from the design tokens) — never reflow.
# Covers both naming conventions: leading `mock-*` (mock-a…e) and trailing `*-mock` (jd-N-…-mock),
# plus anything under a `mocks/` subdir (the docs/research/**/mocks convention).
docs/research/**/mock-*.html
docs/research/**/*-mock.html
docs/research/**/mocks/
